#version 330 core
layout (location = 0) in vec3 vertex;
layout (location = 1) in vec3 color;
out vec3 ColorCoords;

uniform mat4 projection;

void main()
{
    gl_Position = projection * vec4(vertex.xyz, 1.0);
    ColorCoords = color;
}